import { OverviewDashboard } from '@/app/_components/overview';
import Layout from '@/app/_components/navigation';

const page = () => {
	return (
		<Layout>
			<OverviewDashboard />
		</Layout>
	);
};

export default page;
